{% set background_class = content.field_background_color[0]["#markup"] %}

<section class="full-width-bg {{ background_class }}" {{ attributes }}>

      {{ content|without("field_background_color") }}

</section>

{# Include used on node--deadline--full, node--deadline--rich-media-listing, node--deadline--teaser #}

{% set dateString = content.field_end_date[0]["#markup"] %}
{% set hideStartTime = (content.field_hide_start_time[0]["#markup"] == "True") %}

{# Time #}
{% if hideStartTime %}
  {# Show nothing #}
{% else %}  
  <time class="event-time">
    {{ dateString|date("g:i") ~ " " ~ dateString|date("a")|split("")|join(".") ~ "."}}
  </time>
{% endif %}